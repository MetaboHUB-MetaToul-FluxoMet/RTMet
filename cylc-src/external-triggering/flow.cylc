[scheduling]
    initial cycle point = now
    [[xtriggers]]
        catch_raw_1 = catch_raw(hello, 99, qux=True, point=%(point)s, foo=10)
    [[graph]]
        PT1H = "@catch_raw_1 => foo"
[runtime]
    [[foo]]
        script = exit 1